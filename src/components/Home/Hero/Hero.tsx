import { Container } from "@/common/ui";

function Hero() {
    return (
        <section className={"relative"}>
            <Container className={""}>
                <div className={"h-40 bg-blue-200"}></div>
            </Container>
        </section>
    );
}

export default Hero;
